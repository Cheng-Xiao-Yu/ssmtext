<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tedu.weibo.mapper.UserMapper">
    <insert id="insert">
        INSERT INTO user(username,password,nickname,created)
        VALUES(#{username},#{password},#{nickname},#{created})
    </insert>

    <select id="findByUsername" resultType="com.tedu.weibo.pojo.entity.User">
        SELECT id,username,password,nickname,created
        FROM user
        WHERE username=#{username} LIMIT 1
    </select>
</mapper>